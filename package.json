{
  "name": "pupitre",
  "scripts": {
    "build:sidecar": "bun run scripts/build-sidecar.ts",
    "tauri:before-build": "bun run build:sidecar && bun run --cwd ui build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  },
  "private": true,
  "workspaces": ["sidecar", "ui"]
}
